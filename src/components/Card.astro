---
const { title, srcImage, text } = Astro.props;
---

<div class="font-serif flex flex-col gap-2">
  <h3 class="text-2xl">{title}</h3>
  <img
    src={srcImage}
    class="object-fill rounded-full shadow-lg shadow-olivine-700"
  />
  <slot />
</div>
