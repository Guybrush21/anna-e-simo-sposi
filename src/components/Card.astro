---
const { title, srcImage, text } = Astro.props;
---

<div class="flex flex-col gap-2">
  <h3 class="text-2xl uppercase">{title}</h3>
  <img
    src={srcImage}
    class="shadow-buttermilk-700 max-w-full rounded-full object-fill shadow-lg md:max-w-72"
  />
  <slot />
</div>
